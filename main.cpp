#include <iostream>

using namespace std;

void sort(int arr[], int size)
{
    
}

int main()
{
    int arr[] = {11, 29, 8, 99, 2, 18};
    int size = sizeof(arr) / sizeof(int);
    sort(arr, size);
    return 0;
}